#include <minishell.h>

void    tokenization(char *input)
{
    t_token *list;
    int     i;

    
    while (imput[i] && (imput[i] == ' ' || imput[i] == '|t'))
        i++;
    if (!imput)
        break ;
}